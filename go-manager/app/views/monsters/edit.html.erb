<h1>Editing Monster</h1>

<%= render 'form', trainer: @trainer, monster: @monster %>

<%= link_to 'Show', polymorphic_path([@trainer, @monster]) %> |
<%= link_to 'Back', polymorphic_path([@trainer, Monster]) %>
